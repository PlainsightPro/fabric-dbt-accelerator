# Day-to-day work on the dbt project.
#
# What every developer installs, and what every CI/CD pipeline installs. Pinned
# exactly, because the adapter and the linters gate the build.
#
#   pip install -r requirements/requirements.txt
#
# For the one-time provisioning dependencies, see requirements-setup.txt.

dbt-core==1.10.21
dbt-fabric==1.10.0
# dbt-fabric-samdebruyn==1.12.2
sqlfluff==4.2.2
sqlfluff-templater-dbt==4.2.2
dbt-bouncer==3.8.0
